<div id="showoff-content" class="flex flex-col items-center gap-4">
    <div class="text-3xl">
        <span class="font-bold <%= draft_ban.draft_game_user.team == 'team_a' ? 'text-red-500' : 'text-blue-500' %>">
            <%= draft_ban.draft_game_user.user.name %>
        </span>
        Banned
    </div>

    <div class="animate-zoom-in">
        <%= image_tag("perkdecks/#{draft_ban.name}.png") %>
    </div>
</div>